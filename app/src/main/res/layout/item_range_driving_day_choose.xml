<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.ymx.driver.viewmodel.longrangdriving.DayChooseItemViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:gravity="center"
        app:onClickCommand="@{viewModel.selectDay}"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="6dp"
            android:paddingTop="6dp"
            android:paddingLeft="25dp"
            android:paddingRight="25dp"
            android:gravity="center"
            android:background="@{viewModel.rangDrivingDayChoose.select ?@drawable/bg_long_rang_driving_day:@drawable/bg_long_rang_driving_unselect_day}"
            android:singleLine="true"
            android:text="@{viewModel.rangDrivingDayChoose.day}"
            android:textColor="@color/white"
            android:textSize="16sp"
            />

    </LinearLayout>

</layout>